<Page
    x:Class="Herald.StuCurriculum"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Herald"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid x:Name="LayoutRoot">
        <Pivot  x:Name="PivotRoot"
                Margin="0">
            <PivotItem Header="一">
                <Grid>
                    <ListView x:Name="Monday"
                              Height="auto"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="二">
                <Grid>
                    <ListView x:Name="Tuesday"
                              Height="auto"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="三">
                <Grid>
                    <ListView x:Name="Wednesday"
                              Height="1000"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="四">
                <Grid>
                    <ListView x:Name="Thursday"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="五">
                <Grid>
                    <ListView x:Name="Friday"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="六">
                <Grid>
                    <ListView x:Name="Saturday"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="日">
                <Grid>
                    <ListView x:Name="Sunday"/>
                </Grid>
            </PivotItem>
        </Pivot>


    </Grid>
</Page>

